<h2>Iniciar Sesión</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "user") do |f| %>
  <div class="form-group">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control form-control-user", placeholder: "Ingresar Email" %>
  </div>

  <div class="form-group">
    <%= f.password_field :password, autocomplete: "off", class: "form-control form-control-user", placeholder: "Ingresar Contraseña" %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="form-group">
      <div class="custom-control custom-checkbox small">
        <%= f.check_box :remember_me, class:"custom-control-input" %>
        <%= f.label :remember_me, 'Recordar Contraseña', class: "custom-control-label"%>
      </div>
    </div>
  <% end -%>

  <div class="form-group">
    <%= f.submit "Iniciar sesión", class: "btn btn-primary btn-user btn-block" %>
  </div>
<% end %>

 <%= render "devise/shared/links" %>

